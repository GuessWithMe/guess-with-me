const initialState = {
  song: {
    artist: "Linkin Park",
    title: "Somewhere I Belong",
  },
};

const game = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default game;
